import React from 'react';

const UserProfile =(props)=>{

    return(
        <div>
            <p>{props.username}</p>
            <p>{props.username}</p>
            <p>{props.username}</p>
            <p>{props.username}</p>
            <p>{props.username}</p>
            <p>{props.username}</p>
            <p>{props.username}</p>
            <p>{props.username}</p>
            <p>{props.username}</p>
            <p>{props.username}</p>
        </div>
    );
}

export default UserProfile;